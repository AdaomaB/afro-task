[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd server && npm ci --only=production || cd server && npm install --only=production"]

[phases.build]
cmds = ["echo 'Build complete - Node.js backend ready'"]

[start]
cmd = "cd server && node server.js"
